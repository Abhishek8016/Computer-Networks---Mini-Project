# Computer-Networks---Mini-Project
What the project does — clock sync using NTP-style protocol
Architecture diagram — shows client↔server flow with both ports
Key formulas — offset (θ) and delay (δ) with plain explanation
Mandatory requirements table — directly maps each rubric requirement to your code
Packet format — shows low-level binary design
How to run — step by step including multiple clients and SSL status
Sample output — matches exactly what your screenshots show
Tech stack — standard library only, no external packages
